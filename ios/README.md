# iOS plugin output

Run `scripts/build-ios-framework.sh` on macOS with the HBuilder iOS SDK to
generate:

```text
ios/ZhaiwoNativeScan.framework
```

This root folder is the HBuilderX local native plugin folder.
